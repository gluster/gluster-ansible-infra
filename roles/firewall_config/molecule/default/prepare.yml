---
- name: Prepare
  hosts: all
  tasks:
  - name: Install Dependency Packages
    yum:
      name: "{{ packages }}"
      state: latest
    vars:
      packages:
        - firewalld
        - centos-release-gluster
      name: glusterfs-server
